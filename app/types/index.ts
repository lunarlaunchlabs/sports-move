// Export all types
export * from './the-odds-api';

// Wallet type enum for distinguishing between external and invisible wallets
export enum WalletType {
  EXTERNAL = 'external',
  INVISIBLE = 'invisible',
}
